import { SectionHeading } from "./shared";

export function OpenTo() { const options = ["Full-stack and software-development roles", "Internship opportunities", "Freelance web-development projects", "Focused startup and MVP collaborations"]; return <section className="section opportunity-section"><div className="container"><SectionHeading eyebrow="07 / Open to" title="Open to the right next build.">I’m available for opportunities where I can contribute to useful software and keep growing as an engineer.</SectionHeading><ul className="grid gap-3 sm:grid-cols-2">{options.map((option) => <li className="opportunity" key={option}><span>↗</span>{option}</li>)}</ul></div></section>; }
